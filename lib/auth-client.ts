"use client";

// import { createAuthClient } from "better-auth/react";

// export const {
//   signIn,
//   signOut,
//   signUp,
//   useSession,
// } = createAuthClient();

import { createAuthClient } from "better-auth/react";

export const authClient =
  createAuthClient();